<UserControl x:Class="WatchControlLibrary.DragItemList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WatchControlLibrary"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ListBox  x:Name="list" PreviewMouseRightButtonDown="list_PreviewMouseRightButtonDown" PreviewMouseLeftButtonDown="list_PreviewMouseLeftButtonDown">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <TextBlock Text="{Binding DragName}" 
                        AllowDrop="True"
                        DragEnter="TextBlock_PreviewDragEnter"
                        Drop="TextBlock_Drop"
                        DragOver="TextBlock_DragOver"
                        PreviewMouseLeftButtonDown="TextBlock_PreviewMouseLeftButtonDown"
                        PreviewMouseLeftButtonUp="TextBlock_PreviewMouseLeftButtonUp"
                        PreviewMouseMove="TextBlock_PreviewMouseMove"
                        Width="120"
                        >
                    <TextBlock.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Delete" Click="MenuItem_Click"/>
                            <!--<MenuItem Header="成组" Command="{StaticResource ResourceKey=addFragGroup}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=ContextMenu}, Path=Tag}"/>-->
                            <MenuItem Header="Group" Click="MenuItem_Click_1"/>
                            <MenuItem Header="Ungroup" Click="MenuItem_Click_2"/>
                        </ContextMenu>
                    </TextBlock.ContextMenu>
                </TextBlock>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</UserControl>
